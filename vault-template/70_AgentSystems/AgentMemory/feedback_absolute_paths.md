---
name: feedback_absolute_paths
description: 新建或修改文件时必须在回复中引用其绝对路径
type: feedback
---

**Rule:** 当我创建新文件或修改已有文件时，必须在回复消息中写出该文件的**绝对路径**（如 `<YOUR_VAULT_PATH>\xxx.md`），不能只说"已更新"不带路径。

**Why:** 用户需要知道文件在哪，方便手动检查和跳转。

**How to apply:** 每次 Write/Edit 操作后，在回复里写出文件的完整路径。收口时可以汇总列出所有修改过的文件路径。
